import 'package:flutter/material.dart';

class MenuScreenFailure extends StatelessWidget {
  final String failureMessage;
  const MenuScreenFailure({super.key, required this.failureMessage});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
